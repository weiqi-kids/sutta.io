# sutta.io SEO 優化日報 2026-07-15

## 1. 昨日賭注勝負

無新賭注（07-14 為 no-op 日，僅更新記分板）。延續追蹤 07-11 為 reading-guide 加的雙向內鏈：今天是預期首訊窗（4-7 天＝07-15~07-18）第 1 天，用 urlInspection 核對，結果從 07-14 的 `Discovered - currently not indexed` 退回 `URL is unknown to Google`——退步，但沿用已建立的 GSC 索引佇列抖動判斷基準（這批頁本來就會在狀態間來回），首訊窗才第 1 天不下判斷，續等窗口後段（07-16~07-18）。

## 2. 今日判讀摘要

用 GSC urlInspection 對以下追蹤中 URL 逐一核對（共 14 個）：

- **07-03 上線批殘留**：`/topics/four-noble-truths/`（zh）持平 `Discovered - currently not indexed`，繼續觀察至 D+14＝07-17。
- **07-05 上線批 tier5 情境化（6 組）**：今天核對方向較昨日淨進步——`calming-angry-thoughts`(zh)、`five-hindrances`(zh)、`sense-pleasures-danger`(zh/en) 共 4 筆從 `URL is unknown to Google` 轉為 `Discovered - currently not indexed`；`letting-go`(en) 則從 `Discovered - currently not indexed` 退回 `URL is unknown to Google`；其餘（`facing-fear` zh/en、`facing-criticism` zh/en、`five-hindrances` en、`letting-go` zh）持平 `Discovered - currently not indexed`。沿用 07-12 已建立的判斷基準（這批頁本身在 Discovered/unknown 間來回翻動，且皆已有 2-3 條反向連結，非內鏈缺口）——**判定為 GSC 索引佇列正常抖動，非異常，不追加內鏈**，下次該重新評估結構性問題的檢查點是 D+14＝07-19。
- **16 個記分板目標詞**：程式化比對今日 `gsc.topQueries`（21 筆）與 16 個目標詞清單，零命中，全數維持 S0/S1。
- **字典頁 striking-distance**：`kucchi`/`ajaras`/`citta`/`kaya` 連續在榜 ≥5 天、07-01 共用模板批次 14 天冷卻今天（07-15）已解除，但今天這 4 詞**集體跌出**今日 `strikingDistance` 榜單（今日榜單僅剩 `isipatana`(en)，且只第 3 天，未滿 5 天門檻）。站規要求「連續 ≥5 天出現在**當日**榜單」，今天沒有詞同時滿足「仍在榜＋滿 5 天」，判定**無可行動字典頁候選，不動**——07-14 寫的「若仍在榜」本為條件式預告，今天條件未成立，是正常結果非漏做。
- **GA4**：本週 sessions 8（全數 Direct），台灣自然搜尋 0，無 organic／AI referral 新訊號。
- **sitemap（重要觀察）**：`index.sitemaps[].submitted` 從 261 大跳增到 **2856**。查證非異常：07-14 commit `6126a74`（字典頁索引回調，門檻從 7-05 過嚴剪枝回調至 occ≥8，救回約 1.3k 字典頁）已部署，今天 Google 重抓 sitemap 反映此回調生效——是回調換來的收錄回升訊號，07-05 瘦身時定的 227 目標已隨門檻回調作廢。追蹤重點頁 `anapanasati` 今日 `lastCrawlTime` 更新為 07-15（新鮮重抓）。後續觀察這批救回的字典頁 coverage 是否陸續轉 indexed。

## 3. 今日改了哪些檔

**無**。核對過所有追蹤中的檢查點後，沒有一個跨過各自的門檻（reading-guide 首訊窗才第 1 天、four-noble-truths/tier5 皆未到 D+14、字典頁候選今天不滿足「仍在榜＋滿 5 天」雙條件），判定 no-op。只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記今日核對結果與 sitemap 大跳增的查證結論。
- `pages` 更新 7 筆 coverageState（reading-guide zh、letting-go en → URL unknown；calming-angry-thoughts zh、five-hindrances zh、sense-pleasures-danger zh/en → Discovered-not-indexed）。
- `sitemap_watch.current_submitted` 261→2856，新增 07-15 observation 說明跳增原因與目標作廢。

## 4. 明日待做

- **07-16**：reading-guide（zh）首訊窗第 2 天，繼續追是否轉入索引。
- 追 four-noble-truths（zh）D+14＝07-17 到期核對。
- 追 tier5 六組 D+14＝07-19 到期核對；若屆時仍未收錄且 backlink 數量不變，才需重新評估是否為內鏈問題。
- 繼續掃 16 目標詞首次曝光。
- 字典頁 striking-distance：持續觀察是否有詞連續 ≥5 天穩定在榜（今天 isipatana 是第 3 天，若明後兩天持續在榜且仍 0 clicks，07-17 可望達標）。
- 觀察 sitemap 救回的 ~1.3k 字典頁是否開始出現在 coverage 的 indexed 狀態。

## 附註

`data/usage.json`／`data/mn*.json` 今日有 lexicon 用法摘要相關改動（commit `2debb98`，12 條用法摘要），屬 L2/字典補完工作，非本輪 SEO 產出，本輪未觸碰。
