# sutta.io SEO 優化日報 2026-07-05

## 1. 昨日賭注勝負

延續觀察 07-01 字典頁 title/description + DefinedTerm 改動、07-03 主題頁上線。今天 GSC 區間仍是 2026-06-25～2026-07-02，還沒完整涵蓋 07-03 上線後的抓取，平均排名 14.46（昨天 15.84，數字略優但區間重疊、暫不算「進步」，持平看待）。16 個目標詞今天 GSC 查詢清單仍未出現，符合協定 D+2 天的正常狀態，非異常。

## 2. 今日判讀摘要

- 今天用戶已在本 session 之外，於下午另行執行三項重大 SEO 增強（詳見 `2026-07-05-manual-actions.md`，commit 已入庫）：①sitemap 對齊 noindex 收緊（11120→227 URL，去薄留精）②主題頁加 DefinedTerm 結構化資料 ③情境化主題頁 5 頁上線並已全數校稿通過（draft→approved）。這些已涵蓋今天協定中大部分可行動項目。
- 索引狀態（抽查 8 個追蹤頁，較昨天進展）：
  - `/agama/`：**Submitted and indexed**（昨天仍是 unknown，今天已收錄，好訊號）。
  - `/topics/anapanasati/`、`/topics/satipatthana-practice/`、`/topics/anatta/`、`/nikaya/`：由「Google 未知」進到 **Discovered - currently not indexed**（Google 已發現，等待收錄）。
  - `/`、`/topics/`：維持 Submitted and indexed。`/read/mn10/`：既有頁不受影響。
- sitemap 收斂追蹤：今日 JSON 的 `index.sitemaps[].submitted` 仍是 11120（與基準 11030 同量級）。這是預期中的延遲——今天 JSON 於 12:00 UTC 收集，早於下午的 sitemap 瘦身部署與 GSC 重新提交，Google 尚未重抓新版 sitemap。已記入 scoreboard `sitemap_watch.observations`，預期 07-06 起才會看到收斂。
- strikingDistance／highImpZeroClick 清單與昨天重疊，仍是既有字典頁（`tru1` 股票詞、`"determ"` 過濾語法、`parajana` 等）或清單外雜訊詞，依協定第 1 條不進判讀、不觸發動作。
- SLA 檢查點：D+3（07-06）逐頁查收錄，今天（D+2）尚未到期。

## 3. 今日改了哪些檔

無新的 site/ 內容改動——今天可行動的 SEO 訊號（sitemap 收斂、DefinedTerm、情境化頁）已由用戶在本 session 外的另一輪工作中完成並 commit；字典頁 07-01 的 title/description 仍在 14 天冷卻中（依慣例 C2，07-15 前不重改）。依紅線 C2「沒有可行動訊號時 no-op 是常態」，今日大腦層本輪不再重複改動內容。

只更新 `data/seo-daily/scoreboard.json`：
- `pages` 區塊同步今日 GSC 實測收錄狀態：`agama` → Submitted and indexed；`anapanasati`／`satipatthana-practice`／`anatta`／`nikaya` → Discovered - currently not indexed。
- `sitemap_watch.current_submitted` 填入 11120，並新增一筆 observation 說明延遲原因。

build gate（`pnpm -C site build`）全綠（11082 頁，60s 完成）。

**賭注**：無新賭注，延續觀察 07-01 字典頁改動＋07-03 主題頁上線＋今日下午三項增強的自然收錄與排名進展。
**預期效果**：07-06（D+3）起逐頁查收錄，sitemap submitted 數字開始從 11120 往 227 收斂；agama/topics 系列頁陸續從 Discovered 進到 Indexed。

## 4. 明日待做

- 追蹤 sitemap `submitted` 是否開始收斂（11120 → 227 方向）。
- 追蹤 `anapanasati`／`satipatthana-practice`／`anatta`／`nikaya` 是否從 Discovered 進到 Indexed。
- D+3（07-06）到期，逐頁查收錄狀態，未收錄頁視情況 ping（注意今天下午已提前 ping 過一輪，避免重複）。

## 附註

工作目錄另有 `data/mn27.json`／`mn29.json`／`mn30.json`／`mn35.json` 的未提交改動（屬 L2 白話補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit，維持原狀留給對應工作處理。
