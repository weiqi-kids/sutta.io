# sutta.io SEO 優化日報 2026-07-12

## 1. 昨日賭注勝負

07-11 賭注：`/topics/reading-guide/`（zh）新增 2 條反向內鏈（來自 anapanasati、satipatthana），預期 4-7 天內轉入 Submitted and indexed。今天用 GSC urlInspection 核對，仍是 `Discovered - currently not indexed`——只過 1 天，未到預期首訊窗，**不下判斷**，繼續觀察。

07-11「明日待做」的 moggallana 內鏈補強：今天核對前先查現況，發現 `/topics/moggallana/`（zh）已自然從 07-11 的 `Crawled - currently not indexed` 轉為 `Submitted and indexed`——**不需要**再補內鏈，待辦作廢。

## 2. 今日判讀摘要

用 GSC urlInspection 對以下 URL 逐一核對（沿用 `scripts/seo-daily.mjs` 的服務帳號認證邏輯，臨時腳本查詢，未落地為新檔案）：

- **07-03 上線批殘留**：`/topics/four-noble-truths/`（zh）持平 `Discovered - currently not indexed`，繼續觀察至 D+14＝07-17。
- **07-05 上線批 tier5 情境化（6 組：facing-fear/calming-angry-thoughts/facing-criticism/five-hindrances/letting-go/sense-pleasures-danger）今天 D+7 到期**：查每組 `related_topics` 反向連結，6 組皆已有 2-3 條反向連結（非 reading-guide 當初的零連結情況）。多數頁仍 `Discovered - currently not indexed`；`en/letting-go` 從「URL is unknown to Google」轉為 `Discovered - currently not indexed`（進度）；`en/calming-angry-thoughts` 則相反從 `Discovered-not-indexed` 退成「URL is unknown to Google」（判斷為 GSC 索引佇列正常抖動，非異常）。**判定：屬 Google 收錄延遲，非內鏈缺口**（已有 backlink，不同於 reading-guide 當初零連結的明確缺口）——不追加內鏈，繼續觀察至 D+14＝07-19。
- **16 個記分板目標詞**：逐一核對今日 `gsc.topQueries`，仍無任何一個目標詞出現曝光，全數維持 S0/S1。
- **字典頁 striking-distance**：`assosi`(zh,pos5)、`citta`(zh,pos12)、`kaya`(zh,pos6)、`paripuri`(zh,pos6)、`vinnana`(zh,pos9)、`kucchi`(en,pos10.5)、`ajaras`(en,pos6) 連續 7 天(07-06~07-12)在榜、0 clicks，符合站規「連續 ≥5 天 0 clicks」的候選條件。但查證後發現：07-01 commit `60e2f65` 改的是**共用模板** `site/src/pages/lexicon/[key].astro`（及 en 版），一次性對全站 5464 個字典頁的 title/description 生效，並非只動 bhavant 一頁。這批候選頁的實際 title/description 也是同一天（07-01）隨模板一起變的，14 天冷卻同樣算自 07-01 起，**至 07-15 才解禁**，今天不可動（這點記分板先前只記了 bhavant，今天補上這個發現，避免誤觸冷卻期）。
- **`/read/mn22`（蛇喻經）**：連續 7 天 pos6-7、0 clicks，但已有 5 條主題頁反向連結（ananda/anatta/five-aggregates/reading-guide/vipassana）＋既有專屬 title（`蛇喻經（中部22）全文白話對照｜筏喻・無我`，已含關鍵詞），無明確可行動缺口。
- **sitemap**：`index.sitemaps[].submitted` 持平 261（07-11 也是 261），穩定。
- **GA4**：本週 sessions 9（Direct 8、Organic 1），台灣自然搜尋 0；`/topics/moggallana/` 6 views、`/topics/four-noble-truths/` 3 views 有零星流量。

## 3. 今日改了哪些檔

**無**。逐一核對所有可能訊號（striking-distance 字典頁候選卡在共用模板 14 天冷卻、tier5 D+7 已有 backlink 判斷非內鏈缺口、reading-guide 才過 1 天不到判斷窗、目標詞仍零曝光）後，找不到 B2 範圍內可行動且不違反冷卻/慣例的項目，判定 no-op。只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記今日 5 條核對結果與 striking-distance 冷卻期發現。
- `pages` 更新 3 筆 coverageState（moggallana zh→Submitted and indexed；en/letting-go→Discovered-not-indexed；en/calming-angry-thoughts→URL is unknown to Google）。
- `sitemap_watch.current_submitted` 持平 261，新增 07-12 observation。

## 4. 明日待做

- 追 reading-guide（zh）是否在 4-7 天窗（約 07-15~07-18）轉入索引。
- 追 four-noble-truths（zh）D+14＝07-17 到期核對。
- 追 tier5 六組 D+14＝07-19 到期核對；若屆時仍未收錄且 backlink 數量不變，才需重新評估是否為內鏈問題。
- **07-15 起**：字典頁共用模板 14 天冷卻解除，`assosi`/`citta`/`kaya`/`paripuri`/`vinnana`/`kucchi`/`ajaras`（若仍在榜且仍 0 clicks）可依「每天 ≤2 頁字典頁」規則排入 title/description 微調。
- 繼續掃 16 目標詞首次曝光。

## 附註

`data/usage.json` 有未提交改動（`dosa` 詞條，屬 L2/字典補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit。
