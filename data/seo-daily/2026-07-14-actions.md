# sutta.io SEO 優化日報 2026-07-14

## 1. 昨日賭注勝負

無新賭注（07-13 為 no-op 日，僅更新記分板）。延續追蹤 07-11 為 reading-guide 加的雙向內鏈：今天用 GSC urlInspection 核對，仍是 `Discovered - currently not indexed`——今天是第 3 天，預期首訊窗（4-7 天＝07-15~07-18）還沒到，不下判斷。

## 2. 今日判讀摘要

用 GSC urlInspection 對以下追蹤中 URL 逐一核對（共 15 個）：

- **07-03 上線批殘留**：`/topics/four-noble-truths/`（zh）持平 `Discovered - currently not indexed`，繼續觀察至 D+14＝07-17。
- **07-05 上線批 tier5 情境化（6 組，D+7 已於 07-12 到期並判斷為收錄延遲非內鏈缺口）**：今天再核對一次——`facing-fear`(zh/en)、`facing-criticism`(zh/en)、`letting-go`(zh) 持平 `Discovered - currently not indexed`；`calming-angry-thoughts`(en)、`five-hindrances`(en)、`letting-go`(en) 三筆從 `URL is unknown to Google` 轉為 `Discovered - currently not indexed`（進度）；`five-hindrances`(zh)、`sense-pleasures-danger`(zh) 二筆從 `Discovered - currently not indexed` 退回 `URL is unknown to Google`；`calming-angry-thoughts`(zh) 持平 unknown，`sense-pleasures-danger`(en) 由 Discovered-not-indexed 退回 unknown。一進一退。沿用 07-12 已建立的判斷基準（這批頁本身就在 Discovered/unknown 之間來回翻動，且 07-12 D+7 已核過 6 組皆有 2-3 條反向連結，非內鏈缺口）——**判定為 GSC 索引佇列正常抖動，非異常，不追加內鏈**，下次該重新評估結構性問題的檢查點是 D+14＝07-19。
- **16 個記分板目標詞**：程式化比對今日 `gsc.topQueries`（23 筆）與 16 個目標詞清單，零命中，全數維持 S0/S1。
- **字典頁 striking-distance**：今日榜單為 `kucchi`/`ajaras`/`isipatana`（en）＋`citta`/`kaya`（zh）。前 5 個字典頁詞皆屬 07-01 共用模板批次（commit `60e2f65`），14 天冷卻至 07-15 才解禁，還差 1 天，今天不可動。
- **GA4**：本週 sessions 8（全數 Direct），台灣自然搜尋 0，無 organic／AI referral 新訊號（無 chatgpt.com/perplexity.ai 等來源）。
- **sitemap**：`index.sitemaps[].submitted` 持平 261。

## 3. 今日改了哪些檔

**無**。核對過所有追蹤中的檢查點後，沒有一個跨過各自的門檻（reading-guide 首訊窗未到、four-noble-truths/tier5 皆未到 D+14、字典頁候選還差 1 天解冷卻），判定 no-op。只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記今日核對結果（含用 `inspectUrl` 對 tier5/reading-guide/four-noble-truths 共 15 個 URL 的即時查詢）。
- `pages` 更新 6 筆 coverageState（calming-angry-thoughts en、five-hindrances en、letting-go en → Discovered-not-indexed；five-hindrances zh、sense-pleasures-danger zh/en → URL is unknown to Google）。
- `sitemap_watch.current_submitted` 持平 261，新增 07-14 observation。

## 4. 明日待做

- **07-15**：字典頁共用模板 14 天冷卻解除，`kucchi`/`ajaras`/`isipatana`（en）/`citta`/`kaya`（zh，若仍在榜且仍 0 clicks）可依「每天 ≤2 頁字典頁」規則排入 title/description 微調。
- **07-15 起**：reading-guide（zh）進入預期首訊窗（4-7 天＝07-15~07-18），開始追是否轉入索引。
- 追 four-noble-truths（zh）D+14＝07-17 到期核對。
- 追 tier5 六組 D+14＝07-19 到期核對；若屆時仍未收錄且 backlink 數量不變，才需重新評估是否為內鏈問題。
- 繼續掃 16 目標詞首次曝光。

## 附註

`data/usage.json` 有未提交改動（`dosa` 詞條，屬 L2/字典補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit。
