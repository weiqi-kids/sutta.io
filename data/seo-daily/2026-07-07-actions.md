# sutta.io SEO 優化日報 2026-07-07

## 1. 昨日賭注勝負

2026-07-06 大腦層執行**失敗**（`sutta.io-brain.log` 顯示觸發 weekly usage limit，claude 執行逾時/失敗，已由 fallback 保底發出失敗 Slack），當天沒有產出 `2026-07-06-actions.md`，也沒有新賭注可供今天驗證。這是執行環境的一次性限流，非資料異常，不需回退任何內容。

延續觀察 07-05 的三項增強（sitemap 瘦身、DefinedTerm、情境化主題頁上線）：今天有明確的正向訊號，見下段。

## 2. 今日判讀摘要

**里程碑（雙重）：**

- **sitemap 收斂到位**：`index.sitemaps[].submitted` 從 07-05/07-06 的 11120 驟降到 **238**，貼近 07-05 設定的目標值 227（多出 11 筆研判為 `/en/` 鏡像等，非異常）。Google 已重抓瘦身後的 sitemap。
- **追蹤頁全數轉為已收錄**：8 個重點頁（`/`、`/topics/`、`/agama/`、`/nikaya/`、`/topics/anapanasati/`、`/topics/satipatthana-practice/`、`/topics/anatta/`、`/read/mn10/`）今天在 `index.coverage` 中**全部**是 `Submitted and indexed`。比對 07-06 的原始 JSON，`anapanasati`／`satipatthana-practice`／`nikaya` 其實已在 07-06 就轉為已收錄（只是當天大腦層沒跑，沒能及時回報）；今天 `anatta` 補上最後一塊，完成整組。

這印證了 07-05 sitemap 瘦身的假設：砍掉萬計薄字典頁讓 Google 有餘力重新聚焦爬真正的內容頁，換來的收錄回升已經兌現。

**16 個記分板目標詞**：today 的 GSC `topQueries` 逐一比對，**沒有任何一個目標詞出現**（`尼柯耶 中文`／`安般念`／`四念處的修行方法` 等皆仍是 impr_7d=0），16 詞維持 S0，符合協定早期正常狀態，非異常。

**strikingDistance／highImpZeroClick**：清單多為既有英文字典頁的雜訊詞（`kucchi`／`assosi`／`bhavant` 等排名 5-15 但非目標詞），依協定第 1 條不進判讀。唯一貼近內容主題的是 `蛇喻經` → `/read/mn22`（position 6.3、3 次曝光、0 點擊）：title 已是「蛇喻經（中部22）...」高度匹配 query，且該 title 由 07-03 設定，仍在 14 天冷卻期內（07-17 前不可再改），加上只有 3 次曝光統計意義不足，判定不動。

## 3. 今日改了哪些檔

無 site 內容改動（`content/`、`site/src/`、`data/mn*.json` 皆未動）——今天沒有新的、可行動且不違反冷卻/範圍限制的訊號。

只更新 `data/seo-daily/scoreboard.json`：
- `pages` 區塊：`anapanasati`／`satipatthana-practice`／`nikaya`／`anatta` 四頁狀態改為 `Submitted and indexed`。
- `sitemap_watch`：`current_submitted` 226→238，新增 `milestone_reached_date`，補記 07-06（大腦層失敗當天）與 07-07 兩筆 observation。
- `meta.updated` → 2026-07-07，`meta.note` 補記 07-06 失敗原因與 07-07 里程碑。

build gate（`pnpm -C site build`）全綠（11086 頁，70s 完成）。

**賭注**：無新賭注（無新內容改動）。延續觀察：sitemap 是否維持收斂在 227 附近、`/en/` 鏡像頁是否也開始轉入收錄、16 目標詞是否開始出現首次曝光。
**預期效果**：後續幾天持續追蹤 GSC 曝光是否開始出現在目標詞清單上（S0→S1 的第一步）。

## 4. 明日待做

- 確認 `sitemap_watch.current_submitted` 是否穩定在 227 附近（非單日波動）。
- 持續逐日掃 16 目標詞是否首次出現在 `topQueries`（S0→S1）。
- 觀察 07-06 的一次性 weekly limit 是否重演；若連續發生需另行處理排程資源，非本輪 SEO 判讀範圍。

## 附註

工作目錄另有 `data/usage.json` 的未提交改動（`dosa` 詞條，屬 L2/字典補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit，維持原狀留給對應工作處理。
