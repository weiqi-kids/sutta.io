# sutta.io SEO 優化日報 2026-07-08

## 1. 昨日賭注勝負

07-07 無新賭注（無新內容改動，只更新記分板）。延續觀察的兩項今天核對：

- **sitemap 收斂**：`index.sitemaps[].submitted` 07-07 是 238，今天仍是 **238**——完全持平，確認收斂已到位、非單日波動。
- **追蹤頁收錄**：07-07 已全數 `Submitted and indexed` 的 8 個重點頁，今天 `index.coverage` 仍全數維持 indexed，並新增看到 `/lexicon/`、`/en/` 也在已收錄清單中。持平（正向延續），非退步。

## 2. 今日判讀摘要

**16 個記分板目標詞**：逐一核對今日 `gsc.topQueries`，仍無任何一個目標詞出現曝光（`尼柯耶 中文`／`安般念`等 impr_7d=0）。**但**發現記分板本身的狀態機記錄有落後：6 個目標詞對應頁（nikaya、agama、topics/anapanasati、topics/satipatthana-practice、topics/anatta、read/mn10）在 07-06～07-07 已確認轉入 `Submitted and indexed`，但對應的 7 個關鍵字（含共用 anapanasati 頁的「安般念」「觀呼吸」兩詞）狀態欄位仍停留在 `S0`，未跟著頁面收錄轉入協定定義的 `S1（已收錄零曝光）`。今天修正這個記帳落後：7 詞 S0→S1，`state_since=2026-07-08`。這只是狀態機記錄追上事實，不是新訊號，不算「動作」。

**字典頁專屬 striking-distance 規則**（站規特別條款）：
- `bhavant`（`/en/lexicon/bhavant/`）：核對 07-01～07-08 逐日 JSON，**連續 8 天**出現在 strikingDistance（position 穩定第 10 名、impressions 1、clicks 0）。但追查 git log 發現，這頁的 title/description **早在 07-01（commit `60e2f65`）就已經因為同一批訊號（hutva/bhavant/santika/patipada/cetas 等字排名 5-15 零點擊）被改過**——當時把全站字典頁從共用一句話敘述改成用 `entry.gloss` 組每頁專屬 title/description，bhavant 正是當時舉例的詞之一。也就是說今天看到的「連續在榜」其實是**上一輪修正後仍未見效**，而非「還沒修過」。依 C1/C2 同頁 title 14 天冷卻（07-01+14=07-15），**今天不能再動**，記分板已記錄冷卻期限。
- `pamitā`（`/lexicon/panita`）：07-01～07-07 曾連續在榜，**今天跌出** strikingDistance 名單（不在今日 JSON 中），候選作廢，不追蹤。

**其餘 strikingDistance／highImpZeroClick**：其餘為既有英文字典頁雜訊詞（kucchi/assosi/ajaras/nisidi/citta 中文/kāya/paripuri/vedalla/viññāṇa 等），依協定第 1 條非目標詞不進判讀。`蛇喻經 → /read/mn22`（position 6.3、3 曝光、0 點擊）title 07-03 設定，14 天冷卻至 07-17，今天仍不動。

**SLA 日曆**：今天非任何檢查點到期日（D+3=07-06 已過、下一個 D+7=07-10），無強制檢查項。

## 3. 今日改了哪些檔

無 `site/src/`、`content/` 改動（無新的、可行動且不受冷卻限制的訊號；bhavant/蛇喻經皆在冷卻期）。

只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- 7 個關鍵字狀態 S0→S1（尼柯耶 中文、中阿含經白話、安般念、四念處的修行方法、無我 佛教、念處經、觀呼吸），對應頁確認收錄。
- `sitemap_watch.current_submitted` 持平 238，新增今日 observation。
- `meta.note` 補記今日判讀、bhavant 冷卻期限、pamitā 候選作廢。

build gate（`pnpm -C site build`）全綠（11086 頁，68.9s 完成）。

**賭注**：無新賭注（無新內容改動）。

## 4. 明日待做

- 持續觀察 bhavant 是否維持在 striking-distance（冷卻至 07-15 前只能觀察不能改）。
- D+7（07-10）到期：24 個新頁收錄率須查一輪，若未 100% 需補內鏈＋Slack 🟡。
- 持續掃 16 目標詞首次曝光（S1→S2 訊號）。

## 附註

`data/usage.json` 有未提交改動（`dosa` 詞條等，屬 L2/字典補完工作），依紅線 B1 本輪未觸碰、未加入 commit。
