# sutta.io SEO 優化日報 2026-07-13

## 1. 昨日賭注勝負

無新賭注（07-12 為 no-op 日，僅更新記分板）。延續追蹤 07-11 為 reading-guide 加的雙向內鏈：今天用 GSC urlInspection 核對，仍是 `Discovered - currently not indexed`——只過 2 天，未到預期首訊窗（4-7 天＝07-15~07-18），不下判斷，繼續觀察。

## 2. 今日判讀摘要

用 GSC urlInspection 對以下追蹤中 URL 逐一核對：

- **07-03 上線批殘留**：`/topics/four-noble-truths/`（zh）持平 `Discovered - currently not indexed`，繼續觀察至 D+14＝07-17。
- **07-05 上線批 tier5 情境化（6 組，D+7 已於 07-12 到期並判斷為收錄延遲非內鏈缺口）**：今天再核對一次，`facing-fear`(zh/en)、`facing-criticism`(zh/en)、`five-hindrances`(zh)、`letting-go`(zh)、`sense-pleasures-danger`(zh/en) 持平 `Discovered - currently not indexed`；但 `calming-angry-thoughts`(zh)、`five-hindrances`(en)、`letting-go`(en) 三筆從昨天的 `Discovered - currently not indexed` 退回 `URL is unknown to Google`。沿用 07-12 已建立的判斷基準（這批頁本身就在 Discovered/unknown 之間來回翻動，且 07-12 D+7 已核過 6 組皆有 2-3 條反向連結，非內鏈缺口）——**判定為 GSC 索引佇列正常抖動，非異常，不追加內鏈**，下次該重新評估結構性問題的檢查點是 D+14＝07-19。
- **16 個記分板目標詞**：逐一核對今日 `gsc.topQueries`，仍無任何一個目標詞出現曝光，全數維持 S0/S1。
- **字典頁 striking-distance**：今日榜單為 `kucchi`/`ajaras`/`isipatana`（en）＋`citta`/`kaya`（zh）。前 5 個字典頁詞皆屬 07-01 共用模板批次（commit `60e2f65`），14 天冷卻至 07-15 才解禁，還差 2 天，今天不可動。
- **`/read/mn22`（蛇喻經）**：沿用 07-12 判斷（已有 5 條主題頁反向連結＋既有專屬 title，含筏喻/無我關鍵詞），無明確可行動缺口，不動。
- **sitemap**：`index.sitemaps[].submitted` 持平 261。
- **GA4**：本週 sessions 8（全數 Direct），台灣自然搜尋 0，無 organic／AI referral 新訊號。

## 3. 今日改了哪些檔

**無**。核對過所有追蹤中的檢查點後，沒有一個跨過各自的門檻（reading-guide 首訊窗未到、four-noble-truths/tier5 皆未到 D+14、字典頁候選還差 2 天解冷卻），判定 no-op。只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記今日核對結果。
- `pages` 更新 3 筆 coverageState（calming-angry-thoughts zh、five-hindrances en、letting-go en → URL is unknown to Google）。
- `sitemap_watch.current_submitted` 持平 261，新增 07-13 observation。

## 4. 明日待做

- **07-15 起**：字典頁共用模板 14 天冷卻解除，`kucchi`/`ajaras`/`isipatana`（en）/`citta`/`kaya`（zh，若仍在榜且仍 0 clicks）可依「每天 ≤2 頁字典頁」規則排入 title/description 微調。
- 追 reading-guide（zh）是否在 4-7 天窗（07-15~07-18）轉入索引。
- 追 four-noble-truths（zh）D+14＝07-17 到期核對。
- 追 tier5 六組 D+14＝07-19 到期核對；若屆時仍未收錄且 backlink 數量不變，才需重新評估是否為內鏈問題。
- 繼續掃 16 目標詞首次曝光。

## 附註

`data/usage.json` 有未提交改動（`dosa` 詞條，屬 L2/字典補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit。
