# sutta.io SEO 優化日報 2026-07-11

## 1. 昨日賭注勝負

07-10 無新賭注（大腦層當天無 `actions.md`，屬 no-op/中斷，reflect 層當天仍正常跑經營層優化 commit `a6d6f4a`，非資料異常）。延續觀察兩項今天核對：

- **sitemap 收斂**：`index.sitemaps[].submitted` 07-09 是 242，今天 **261**——小幅波動（+19），仍貼近 227 目標區間，非異常。
- **8 個核心追蹤頁**（`/`、`/topics/`、`/agama/`、`/nikaya/`、`/topics/anapanasati/`、`/topics/satipatthana-practice/`、`/topics/anatta/`、`/read/mn10/`）今天在 `index.coverage` 中仍全數 `Submitted and indexed`，持平。

## 2. 今日判讀摘要

**16 個記分板目標詞**：逐一核對今日 `gsc.topQueries`，仍無任何一個目標詞出現曝光，全數維持 S0/S1。

**D+7 檢查點補查（原到期日 07-10，今日 D+8，昨天漏查）**：用 GSC `urlInspection` API 對 `scoreboard.pages` 中標記 `unknown` 的 40 個新頁逐一查詢（服務帳號金鑰同 `scripts/seo-daily.mjs` 使用的那把）。結果：

- **07-03 上線批（tier1-4，共 37 頁，D+7 早已到期）**：34 頁已 `Submitted and indexed`。3 頁仍未收錄：
  - `/topics/four-noble-truths/`（zh）：`Discovered - currently not indexed`。已在首頁精選 `HOME_FEATURED_QUESTIONS` 且有 8 條主題頁互鏈指向它（`related_topics` 反查），內鏈已充分，判斷是 Google 索引評估延遲、非內鏈缺口，**不動**，觀察至 D+14（07-17）。
  - `/topics/reading-guide/`（zh）：`Discovered - currently not indexed`。查 `related_topics` 反向連結發現**掛零**——沒有任何主題頁連回它，只能靠 `/topics/` 目錄頁單一入口，是明確的內鏈缺口。
  - `/topics/moggallana/`（zh）：`Crawled - currently not indexed`。僅 1 條反向連結（來自 `ananda`）。
- **07-05 上線批（情境化 tier5：facing-fear/calming-angry-thoughts/facing-criticism/five-hindrances/letting-go/sense-pleasures-danger）**：D+7 = 07-12，明天才到期，目前多為 `Discovered - currently not indexed` 屬預期中，非異常；`letting-go`（en）目前是 `URL is unknown to Google`，晚一天再看。

## 3. 今日改了哪些檔

依協定「D+7 仍未收錄補內鏈」＋「每天最多推 2 頁內鏈」，鎖定內鏈缺口最明確的 `reading-guide`（four-noble-truths 內鏈已足夠不需再加；moggallana 留給明天，今天已達 2 頁上限）：

- `content/topics/anapanasati.json`：`related_topics`（zh+en）加回 `reading-guide`（原本 reading-guide 已單向連到 anapanasati，現改雙向）。
- `content/topics/satipatthana.json`：`related_topics`（zh+en）加回 `reading-guide`（同上，改雙向）。

只動 `related_topics` 這個導覽用結構欄位，未動人工校稿過的 `definition`/`sections`/`faq` 等敘述內文（守慣例 C1）。

`node scripts/validate-topics.mjs` 通過；`pnpm -C site build` 全綠（11150 頁，66.65s），並用 grep 確認 `dist/topics/anapanasati/`、`dist/topics/satipatthana/` 渲染出的頁面確實含 `href="/topics/reading-guide/"`。

只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記 07-10（無 actions.md、sitemap 242→261）與今日 D+7 補查全文。
- `pages` 40 個 `unknown` 條目全部填入本次查到的真實 `coverageState`。
- `sitemap_watch.current_submitted` 242→261，新增 07-10、07-11 兩筆 observation。

**賭注**：`/topics/reading-guide/`（zh）新增 2 條反向內鏈（來自已收錄的 anapanasati、satipatthana）。預期：提升 crawl 訊號，下次檢查（約 4-7 天後）`coverageState` 轉為 `Submitted and indexed` 或更接近。非事實型內容改動，不受鐵則 1 約束。

## 4. 明日待做

- 內鏈補強輪到 `/topics/moggallana/`（今天因每日 2 頁上限延後）。
- 持續觀察 `/topics/four-noble-truths/`（zh）是否在 D+14（07-17）前自然轉入索引；若到期仍 `Discovered-not-indexed`，依協定 Slack 🟡。
- 07-05 批（tier5 情境化 6 組×zh/en）D+7 明天（07-12）到期，需正式核對一輪（今天已提前用 urlInspection 摸過底，多數 `Discovered-not-indexed` 符合預期）。
- 繼續掃 16 目標詞首次曝光；`bhavant`／其他共用模板字典頁 14 天冷卻至 07-15 屆滿後可重新評估 CTR 微調。

## 附註

`data/usage.json` 有未提交改動（`dosa` 詞條，屬 L2/字典補完工作，非本輪 SEO 產出）。依紅線 B1（不碰經文內容），本輪未觸碰、未加入 commit。
