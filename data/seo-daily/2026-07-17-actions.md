# sutta.io SEO 優化日報 2026-07-17

## 1. 昨日賭注勝負

無新賭注（07-16 為 no-op 日）。延續追蹤 07-11 為 reading-guide 加的雙向內鏈：今天是預期首訊窗（4-7 天＝07-15~07-18）第 3 天，持平 `Discovered - currently not indexed`（07-16 已從 unknown 轉正，今天維持），窗口未到末端（07-18），不下判斷，續等明天。

## 2. 今日判讀摘要

用同一支 GSC urlInspection 呼叫（沿用 seo-daily.mjs 的 auth/inspect 邏輯寫成一次性腳本）對 reading-guide/four-noble-truths/tier5 情境化批共 14 個 URL 逐一核對：

- **four-noble-truths（zh）D+14 到期核對**：14 天後仍 `Discovered - currently not indexed`。依協定重新評估是否為內鏈缺口——實查 `related_topics`：8 個主題頁（dependent-origination/letting-go/noble-eightfold-path/reading-guide/sariputta/five-aggregates/sense-pleasures-danger/marana）zh+en 皆已反向連回本頁，且已在首頁精選 HOME_FEATURED_QUESTIONS（07-11 已核實）——內鏈已充分，非缺口，重新確認 07-11 建立的判斷（Google 索引評估延遲，非結構性問題），今天不追加內鏈。
- **07-05 上線批 tier5 情境化（6 組）**：今天全數 12 個 zh/en 頁一致收斂為 `Discovered - currently not indexed`，不再有任何「URL is unknown to Google」——近日在兩狀態間抖動的幾組今天全部轉正，較昨日淨進步。D+14=07-19 才是下次重新評估結構性問題的檢查點，繼續觀察。
- **16 個記分板目標詞**：程式化比對今日 `gsc.topQueries`（22 筆），仍全數零曝光，S0/S1 未變。留痕一則：查詢「四念處」（1 次曝光，position 142，對應 `/topics/satipatthana/`）首次出現，與追蹤詞「四念處是什麼」字面不同、不觸發狀態轉換，僅記錄觀察是否為該目標詞的前兆訊號。
- **字典頁 striking-distance（今日動手）**：`/en/lexicon/isipatana/` 連續在榜滿 5 天（07-13~07-17，position 11，0 clicks），達站規門檻；其 title/description 生成邏輯（`site/src/pages/en/lexicon/[key].astro`）最後改動於 07-01（commit 60e2f65），已過 14 天冷卻。查出真因：共用模板優先取 `entity.name_zh`（中文），導致英文詞條頁標題混入中文——`Isipatana (仙人墮處（仙人住處）) ｜Lexicon`，對英文搜尋者是語言不匹配的怪異標題。`data/lexicon.json` 該詞已有既存英文 DPD gloss `"name of a forest near Bārāṇasī"`（既有資料，非新造），故修正模板：新增 `/^name of/i` 判斷式，符合者優先用英文 gloss，否則維持原 `entity.name_zh` 後備——避免誤傷 `ananda`（gloss 為「happiness; joy」，是巴利文常用詞義非人名解釋，若一併替換會把「阿難」頁標題錯置成「快樂；喜悅」）。此為共用模板單檔改動，影響全站 27 個 entity 詞條中 24 個（另 3 個 ananda/ukkattha/migaramata 因 gloss 不符 pattern 或無 gloss，不受影響）。
- **highImpZeroClick**：`/en/topics/anatta/` 對應 anatta／anatta buddhism／anatta meaning（4/3/3 次曝光），持平沿用 07-16 判斷（position 88-100，屬排名問題非 CTR 問題，且 EN 非優先受眾），不動。
- **GA4**：本週 sessions 1（全 Direct），台灣自然搜尋 0，無 organic／AI referral 新訊號。
- **sitemap**：`index.sitemaps[].submitted` 持平 2856。今日改動不影響 sitemap 收錄範圍（僅改呈現，非新增/刪除頁）。

## 3. 今日改了哪些檔（共 1 個，未達 5 檔上限）

- `site/src/pages/en/lexicon/[key].astro`：修正英文字典 entity 詞條 title/description 的 meaning 選字邏輯，優先用既有英文 DPD gloss（符合 `/^name of/i`）取代誤植的中文 `entity.name_zh`。

賭：`/en/lexicon/isipatana/` 的 CTR（目前 position 11、0 clicks）。預期效果：標題從中英夾雜的怪異樣式（`Isipatana (仙人墮處（仙人住處）)`）改為純英文（`Isipatana (name of a forest near Bārāṇasī)`），更貼近英文搜尋者預期，拉高點擊率；連帶影響全站另 23 個 entity 英文詞條頁（如 sariputta/savatthi/jetavana 等）同樣受益。

- gate：`pnpm -C site build` 全綠（11318 頁）。已抽查 `isipatana`/`ananda`/`sariputta` 三頁輸出，確認 isipatana 轉純英文、ananda 維持中文（未誤傷）、sariputta 顯示完整英文 gloss。
- commit：`[auto-claude-seo] 2026-07-17: <訊息>`，push 後跑 `scripts/seo-index-ping.mjs` 通報 `https://sutta.io/en/lexicon/isipatana/`。

## 4. 明日待做

- **07-18**：reading-guide（zh）首訊窗（4-7 天＝07-15~07-18）最後一天，屆時若仍未轉為 Submitted/Crawled 以上狀態，下判斷（是否需要重新評估內鏈）。
- 追 tier5 六組 D+14＝07-19 到期核對；今天已全數收斂為 Discovered-not-indexed，若 07-19 仍未轉 indexed 才需重新評估。
- 繼續觀察「四念處」query 是否持續出現、是否帶動追蹤詞「四念處是什麼」曝光。
- 觀察 isipatana(en) CTR 改動後 4-7 天內是否有點擊/排名變化（下次檢查點 07-21~07-24）。
- 繼續掃 16 目標詞首次曝光；觀察 sitemap 救回的 ~1.3k 字典頁是否開始出現 indexed 狀態。
