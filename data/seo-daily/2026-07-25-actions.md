# sutta.io SEO 優化日報 2026-07-25

## 1. 昨日賭注勝負

07-24 為 no-op 日（D+21 SLA 檢查點核對＋尼柯耶訊號連續第 2 天觀察），無新賭注。沿用 07-17 isipatana(en) 標題改動的觀察：仍在 14 天冷卻期至 07-31，效果尚未到判斷窗。

## 2. 今日判讀摘要（本日重點：尼柯耶／小乘佛教經典訊號達成行動門檻，對 /nikaya/ 集中加碼）

- **尼柯耶／小乘佛教經典訊號連續第 3 天出現，達成 07-23 訂下的「連續 ≥3-5 天」行動門檻**：本日範圍 07-15~07-22（較昨日 07-14~07-21 前移一天）——
  - 「尼柯耶」：impressions 3、position 6.33、clicks 1，與昨日（07-24）數字完全持平，代表訊號持續存在但今天未進一步增強。
  - 「小乘佛教經典」：impressions 2→4（續增），position 持平第 1 名，clicks 仍 0（此組合同時列入今日 `highImpZeroClick`——第 1 名卻 0 點擊；但累計曝光僅 4 次，樣本量太薄，暫不視為需要改標題的 CTR 問題，故不動 title/description，避免用單日薄資料驅動 14 天冷卻的改動）。
  - GA4 `taiwanOrganicSessions` 本週=3，持續非零（07-22:2、07-23:1、07-24:0 的滾動 7 天窗）。
  - 時間軸：07-23 首見（day1）→ 07-24 增強（day2）→ 07-25 持穩（day3）。已連續 3 天出現，達 07-23 訂下的門檻下限。
- **執行的加碼動作（B2 範圍內，內部連結）**：檢查全站對 `/nikaya/` 的內鏈現況，發現除主導覽（nav）、`/browse`、`/world` 已連結外，**全站權重最高的首頁完全沒有連到 `/nikaya/`**（首頁「或直接使用工具」入口區塊只有瀏覽中部／搜尋／字典／早期佛典的世界 4 個入口，缺尼柯耶導覽）。於 `site/src/pages/index.astro` 新增第 5 個入口連到 `/nikaya`，沿用既有 `.entry`/`.entry-label` 樣式與既有 i18n 字串 `t.nav.nikaya`（「尼柯耶導覽」），零新增 CSS、零新增 i18n key。
  - 未動內容段落——`/nikaya/` 的 FAQ（含「尼柯耶是小乘佛教經典嗎？」）與定義段落已於 07-24 由反思層補過，今日只補內鏈，不重複動內容，維持每頁 title 14 天冷卻慣例（title 本身這次也未動）。
  - 未動 `/en/` 首頁（英文為鏡像非軸線，不投入資源）。
  - 「尼柯耶」字面仍與追蹤詞「尼柯耶 中文」不同，不觸發 16 目標詞正式 S0→S1/S2 狀態轉換（沿用既有嚴格字面比對基準）。
- **16 個記分板目標詞**：程式化比對今日 `gsc.topQueries`，仍無字面完全相符者，`scoreboard.keywords.best_pos` 全數維持 null。
- **4 垂直對比第 22 天**（觀察窗至約 08-11，未到判讀期）：marana(zh) 持平已收錄；facing-fear(zh) 持平 Discovered-not-indexed；facing-criticism(zh) 持平 URL is unknown to Google；letting-go(zh) 持平 Discovered-not-indexed。與 07-24 一致，非新抖動。
- **字典頁 striking-distance**：今日榜單 6 筆——`/en/lexicon/atthasi/`（pos10.7，第 3 天）、`/nikaya`「尼柯耶」（pos6.33，第 3 天，見上，已行動但非因 striking-distance 規則而是因訊號門檻規則）、`/lexicon/`「medi dvi nude」（雜訊查詢）、`/lexicon/kumma/`、`/lexicon/sukkha/`、`/topics/anapanasati/`「mn118」（pos5，第 2 天）。皆未滿站規「連續 ≥5 天在榜」門檻，字典頁 title/description 本身不動。
- **highImpZeroClick**：`/en/lexicon/atthasi/`（7 次曝光）與 `/nikaya`「小乘佛教經典」（4 次曝光，pos1，見上，暫不動標題）。
- **sitemap**：`index.sitemaps[].submitted` 2858→2858（持平）。

依「站內已飽和，瓶頸在站外」姿態，四項領先指標：①任一目標詞首次非零曝光（尼柯耶/小乘佛教經典語意相關訊號已達 3 天門檻並觸發今日內鏈加碼，字面仍未觸發 16 詞正式狀態轉換）、②GA4 首個 organic/referral session（台灣中文版本已連續多天出現，本週累計 3）、③長期卡住新頁成批轉 indexed（未觸發）、④GSC 新反向連結（未查到）。

## 3. 今日改了哪些檔（1 個站台檔）

- `site/src/pages/index.astro`：首頁「或直接使用工具」入口區塊新增連到 `/nikaya/` 的第 5 個入口（內部連結加碼，見上）。

- gate：`pnpm -C site build` 綠燈（11318 頁全數建置成功，exit 0）。
- 視覺驗證：`site/scripts/shot.mjs` 依賴的 playwright 在本機環境符號連結損壞（`node_modules/playwright` 指向 2026-07-09 某互動 session 的暫存路徑 `/tmp/claude-0/.../scratchpad/shot/node_modules/playwright`，該路徑已不存在，且 playwright 本非 `site/package.json` 宣告的依賴——這是環境本身的既有缺口，非本次改動造成）。無法補跑正式截圖；改動本身零新增 CSS、完全重用首頁另外 4 個入口已上線驗證過的 `.entry`/`.entry-label` 樣式，風險評估極低，但如實記錄「未跑螢幕截圖」，不宣稱已完成視覺驗證。
- commit：`[auto-claude-seo] 2026-07-25: 尼柯耶訊號達3天門檻，首頁補內鏈至/nikaya/`。
- push 後對 `https://sutta.io/` 與 `https://sutta.io/nikaya/` 執行 `scripts/seo-index-ping.mjs` 通知 Google/IndexNow 重新收錄。

## 4. 明日待做

- 核對「尼柯耶」「小乘佛教經典」查詢是否持續出現（第 4 天）、`/nikaya/` 排名是否因今日新增內鏈而有變化（GSC 有 2-3 天延遲，效果最快 07-27~07-28 才看得到）；GA4 `taiwanOrganicSessions` 是否持續非零。
- 追蹤今日首頁內鏈改動對 `/nikaya/` 的 impressions/position 影響，作為本次賭注的驗證對象。
- 若環境許可（例如互動 session 重新安裝 playwright），補跑一次首頁截圖做事後視覺確認。
- 繼續掃 16 目標詞首次曝光（字面比對）。
- 繼續觀察 isipatana(en) 14 天冷卻至 07-31。
- 4 垂直對比持續觀察至觀察窗末（約 08-11），不提前下結論。
- D+30（08-02）SLA 檢查點：第一梯隊 5 詞至少 2 詞有曝光、至少 1 詞 pos≤15。
