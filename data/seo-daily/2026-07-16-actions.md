# sutta.io SEO 優化日報 2026-07-16

## 1. 昨日賭注勝負

無新賭注（07-15 為 no-op 日）。延續追蹤 07-11 為 reading-guide 加的雙向內鏈：今天是預期首訊窗（4-7 天＝07-15~07-18）第 2 天，用 urlInspection 核對，結果從 07-15 的 `URL is unknown to Google` 轉為 `Discovered - currently not indexed`——窗口內第一個正向訊號，但窗口未到末端（07-18），不下判斷，續等。

## 2. 今日判讀摘要

今日 `seo-daily.mjs` 收集的 JSON 中 `index.coverage` 只含固定 5 個追蹤 URL（不含 reading-guide/four-noble-truths/tier5 這批），故另用同一套服務帳號認證直接呼叫 GSC urlInspection API 對這 14 個 URL 逐一核對（做法沿用歷史慣例）：

- **07-03 上線批殘留**：`/topics/four-noble-truths/`（zh）持平 `Discovered - currently not indexed`，繼續觀察至 D+14＝07-17。
- **07-05 上線批 tier5 情境化（6 組）**：`calming-angry-thoughts`(en)、`letting-go`(en) 轉為 `Discovered - currently not indexed`（進步）；`calming-angry-thoughts`(zh)、`five-hindrances`(zh)、`sense-pleasures-danger`(en) 退回 `URL is unknown to Google`（退步）；其餘（`facing-fear` zh/en、`facing-criticism` zh/en、`five-hindrances` en、`letting-go` zh、`sense-pleasures-danger` zh）持平 `Discovered - currently not indexed`。一進一退，沿用 07-12 已建立判斷基準（6 組皆已有 2-3 條反向連結，非內鏈缺口）——**判定為 GSC 索引佇列正常抖動，非異常，不追加內鏈**，下次重新評估結構性問題的檢查點是 D+14＝07-19。
- **16 個記分板目標詞**：程式化比對今日 `gsc.topQueries`（21 筆，皆英文雜訊詞如 anatta/garu meaning 等），零命中，全數維持 S0/S1。
- **字典頁 striking-distance**：今日榜單僅 `isipatana`(en)，position 11，0 clicks，連續在榜第 4 天（07-13 起），未滿站規「連續 ≥5 天」門檻，今天不可動。
- **highImpZeroClick**：`/en/topics/anatta/` 對應 `anatta`／`anatta buddhism`（4／3 次曝光，0 clicks），但該頁排名皆在 80-93 名，比照字典頁 striking-distance 條款「position>15 屬排名問題非 CTR 問題」的判斷基準，且站台受眾鎖定中文（EN 非優先受眾），不動。
- **GA4**：本週 sessions 6（全數 Direct），台灣自然搜尋 0，無 organic／AI referral 新訊號。topPages 前幾名為 `/topics/`、`/topics/moggallana/`、`/`，均屬 Direct 流量，非搜尋訊號。
- **sitemap**：`index.sitemaps[].submitted` 持平 **2856**（07-15 大跳增後穩定，非單日波動）。繼續觀察 07-14 門檻回調救回的 ~1.3k 字典頁是否陸續轉 indexed（目前追蹤清單未涵蓋個別字典頁，暫無法直接量測）。

## 3. 今日改了哪些檔

**無**。核對過所有追蹤中的檢查點後，沒有一個跨過各自的門檻（reading-guide 首訊窗才第 2 天、four-noble-truths/tier5 皆未到 D+14、字典頁候選未滿 5 天在榜門檻、highImpZeroClick 屬排名問題非 CTR 問題），判定 no-op。只更新 `data/seo-daily/scoreboard.json`（留痕檔，不計入 ≤5 檔上限）：
- `meta.note` 補記今日核對結果。
- `pages` 更新 14 筆 coverageState。
- `sitemap_watch.current_submitted` 持平 2856，新增 07-16 observation。

## 4. 明日待做

- **07-17**：four-noble-truths（zh）D+14 到期，核對是否需要重新評估內鏈缺口。
- reading-guide（zh）首訊窗第 3 天，繼續追是否延續今天的正向轉折。
- 追 tier5 六組 D+14＝07-19 到期核對；若屆時仍未收錄且 backlink 數量不變，才需重新評估是否為內鏈問題。
- 繼續掃 16 目標詞首次曝光。
- 字典頁 striking-distance：`isipatana`(en) 若明後兩天持續在榜且仍 0 clicks，07-17 可望滿 5 天門檻。
- 觀察 sitemap 救回的 ~1.3k 字典頁是否開始出現 indexed 狀態。
